Template.mealSelection.events({
  'submit form': function(e, template){
    e.preventDefault();
    console.log('not currently implemented');
    // var meal = [
    //   template.find('').value,
    // ]
    // Guests.update({_id: Session.get('currentUserId')}, {$set: {meal: meal}})
  }
});
